<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <application
            android:name="com.tencent.mps.srplayer.SRApplication"
            android:allowBackup="false"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:hardwareAccelerated="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.SRPlayer"
            android:usesCleartextTraffic="true"
            tools:targetApi="35">

        <activity
                android:name=".HomeActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".demo.bytebuffer.ByteBufferActivity" />

        <activity android:name=".demo.bytebuffer.ByteBufferBaseActivity" />

        <activity android:name=".demo.texture.TextureOesActivity" />

        <activity android:name=".demo.texture.TextureOutputActivity" />

        <activity android:name=".demo.texture.Texture2dActivity" />

        <activity android:name=".demo.bytebuffer.ByteBufferSrActivity" />

        <activity android:name=".demo.surface.TieSurfaceActivity" />
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <application
            android:name="com.tencent.mps.srplayer.SRApplication"
            android:allowBackup="false"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:hardwareAccelerated="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.SRPlayer"
            android:usesCleartextTraffic="true"
            tools:targetApi="35">

        <activity
                android:name=".HomeActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".demo.bytebuffer.ByteBufferPathActivity" />

        <activity android:name=".demo.bytebuffer.ByteBufferPathBaseActivity" />

        <activity android:name=".demo.oestexture.OesTexturePathActivity" />

        <activity android:name=".demo.bytebuffer.ByteBufferSrPathActivity" />

        <activity android:name=".demo.enhancesurface.EnhanceSurfacePathActivity" />

        <activity android:name=".demo.tsrpass.TsrPassDemoActivity" />
    </application>

</manifest>